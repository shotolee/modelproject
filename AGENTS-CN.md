# 项目 Agent 规则（中文）

`AGENTS.md` 是 Agent 规则入口，本文件为中文配套说明。两者保持一致，发现矛盾应报告。

流程：产品需求 → Codex 设计与计划 → OpenCode + MiniMax M3 实施 → OpenCode + Kimi 独立评审 → Codex 终审。模型名称只约定职责，不自动切换或调度模型。

## 产品需求

`docs/requirements/product.md` 描述项目是什么、谁使用、解决什么问题、有哪些功能、约束和范围。

- 用户可以口述、粘贴材料或分批补充，Codex 负责整理文档。不要求用户写详细规格、需求编号、验收编号或版本记录。
- 默认使用一个产品文档，不自动拆功能文件、建需求索引、基线清单或追踪矩阵。
- 功能细化、流程、数据模型、接口、模块、实施步骤及可测试的验收条件，由 Codex 写入架构和实施计划。
- 保留用户的业务意图，不替用户猜业务规则。只追问影响当前工作的关键问题；已有明确指令不重复确认。
- product.md 描述当前预期行为，不记录实现进度；未来想法及待确认问题与本轮范围分开。
- 重要新增、修改或移除由 Codex 同步 product.md，并在 change-log.md 按日期简记变化及原因，必要时补充影响。详细差异交给 Git，不维护每条需求的版本号。
- 需求变更后先分析架构影响，再更新架构、计划和必要的技术决策，不能为了方便编码偷偷改需求。

## 设计与计划 — Codex

读取产品需求、近期变更、已有代码和项目文档，为本轮选择适量范围。负责架构、具体步骤、涉及文件、验收条件及验证方式，注明本轮不做什么。架构与计划可以一起完成；本轮范围明确且无阻塞问题后将计划设为 Ready，在业务编码前停止。

## 实施 — OpenCode + MiniMax M3

读取产品需求、变更记录、架构、实施计划和技术决策。

- 计划为 Ready 且收到实施指令后开始。
- 按计划实施，不改业务意图、架构、验收或范围，不做无关重构。
- 每个有意义的阶段执行适用检查，在计划里记录命令、实际结果、修改文件与遗留问题。
- 需求变化或设计冲突时暂停受影响部分，交回 Codex；独立工作可继续，不能混用新旧范围或自行重新设计。
- 完成后交独立评审，未运行的检查不能写成通过。

## 独立评审 — OpenCode + Kimi

不修改代码，只写 `docs/review-report.md`。直接核对 product.md 的相关功能，以及架构和计划；关注正确性、遗漏、回归、边界、错误处理、安全、测试和不必要复杂度。

按 Critical / Major / Minor / Suggestion 分类，给出位置、证据、影响和建议，用功能名称或章节关联需求即可。记录评审版本与限制，修复后复核再关闭问题。普通错误回实施，设计冲突回 Codex。

## 终审 — Codex

写入 `docs/audit-report.md`，先给结论，不直接改代码。检查业务意图、架构边界、技术债、复杂度、文档一致性、测试和评审问题处置。本轮验证完成且无阻塞时才设 Done；单轮完成不表示整个产品完成，也不自动授权部署。

## 依据顺序

1. `docs/requirements/product.md` 的当前需求（不含未来想法和待确认问题）
2. `docs/architecture.md`
3. `docs/decisions.md` 中当前有效的决策
4. `docs/implementation-plan.md`
5. 现有实现

发现矛盾须报告；此顺序不覆盖用户明确指令及运行环境的上级规则。变更日志解释历史，product.md 描述当前目标。

## Git 与交接

修改前检查 Git 状态及相关文件，保留无关用户改动。完成前执行适用检查，总结文件、实际结果及遗留问题。

- 计划记录实施前 Git commit，供代码评审确定范围，不增加需求审批流程。新仓库无提交时明确检查全部初始文件，不因空 diff 判定通过。
- 评审覆盖起点以来的已提交变化、暂存、未暂存及未跟踪文件，新增文件需实际读取。
- 替换旧计划和报告前确保 Git 历史已保存；尚未提交则保留带日期的副本，无需归档索引或任务编号体系。新任务重置评审和审计结论。
- 未获授权不提交、推送、部署或执行破坏性 Git 操作；模板不写密钥及机器专属私人路径。
- 未收到明确指令，不自动派发其他 Agent 或推进下一阶段。
